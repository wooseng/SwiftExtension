# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'SwiftExtension' do
  use_frameworks!

  pod 'HandyJSON' # JSON序列化与反序列化
  pod 'MJRefresh' # 下拉刷新

  target 'SwiftExtensionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftExtensionUITests' do
    # Pods for testing
  end

end
